apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: cv-gateway-monitoring
  namespace: cvalyze
  labels:
    app: cv-gateway
spec:
  selector:
    matchLabels:
      app: cv-gateway
  endpoints:
  - port: 8000
    path: /metrics
    interval: 30s
    timeout: 10s