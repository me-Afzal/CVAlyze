apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: cv-user-monitoring
  namespace: cvalyze
  labels:
    app: cv-user
spec:
  selector:
    matchLabels:
      app: cv-user
  endpoints:
  - port: 8001
    path: /metrics
    interval: 30s
    timeout: 10s